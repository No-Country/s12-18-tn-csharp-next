/**
 * Ruta principal de las rutas de información de usuario de la api.
 */
export const BASE_API_ME_PATH = "/me";

/**
 * Rutas de la api de información de usuario.
 */
export enum ME_API_ROUTES {
    EVENTS = "/events"
}