/**
 * Ruta principal de las rutas de autenticación de la api.
 */
export const BASE_API_AUTH_PATH = "/auth";

/**
 * Enumeración de las distintas rutas de la api de autenticación.
 */
export enum API_AUTH_ROUTES {
    REGISTER = "/register",
    LOGIN = "/login"
}